import { JwtTokensInterface } from '..';
export declare function getHeaders(jwtTokens: JwtTokensInterface): Promise<{
    tokens: JwtTokensInterface;
    headers: {
        Authorization: string;
        'Content-Type': string;
    };
}>;
