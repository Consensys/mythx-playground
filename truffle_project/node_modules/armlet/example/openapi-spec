#!/usr/bin/env node
'use strict'

const armlet = require('../index') //if armlet is not installed
// const armlet = require('armlet')  // if armlet is installed

armlet.OpenApiSpec().then(
  result => console.log(result)
)
